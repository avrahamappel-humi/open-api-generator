{
    "scripts": {
        "validate-test-specs": "for file in tests/fixtures/*.yml; do echo \"Checking $file\"; node_modules/.bin/swagger-cli validate $file; done"
    },
    "devDependencies": {
        "@apidevtools/swagger-cli": "^4.0.4",
        "@prettier/plugin-php": "^0.17.6",
        "prettier": "^2.4.1"
    },
    "prettier": {
        "printWidth": 120,
        "tabWidth": 4,
        "singleQuote": true,
        "trailingCommaPHP": true
    }
}
